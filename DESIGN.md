# DESIGN.md

## Design Concept

**Guided Compliance Workspace**

CBAM Local은 CBAM 제출 준비를 단계별로 안내하는 로컬 우선 업무 콘솔이다. 화면은 계산기를 예쁘게 포장하는 것이 아니라, 기업 담당자와 컨설턴트가 “지금 무엇을 해야 하는지, 왜 막혔는지, 어디로 가야 하는지”를 즉시 알 수 있게 해야 한다.

## Users

- 국내 중소·중견기업 CBAM 실무 담당자
- 생산, 환경, 무역, 구매 부서 담당자
- CBAM 컨설턴트

기본 화면은 기업 담당자를 기준으로 설계한다. 컨설턴트에게 필요한 상세 수치와 테이블은 제공하되, 첫 화면과 주요 CTA는 다음 작업 안내 중심으로 둔다.

## Product UX Principles

- 계산식보다 다음 작업을 먼저 보여준다.
- 경고보다 해결 방법을 먼저 보여준다.
- 테이블보다 단계별 준비 상태를 먼저 보여준다.
- Export는 다운로드 화면이 아니라 제출 전 점검 게이트로 보여준다.
- 사용자는 첫 화면에서 “다음 작업 계속하기”를 찾을 수 있어야 한다.
- 회사 CBAM 데이터는 로컬 우선 흐름으로 다룬다.
- 무료 라이선스, 공지, 업데이트 확인은 CBAM 입력 데이터 수집으로 이어지면 안 된다.

## Visual Tone

- 차분한 B2B SaaS
- 규제 대응 업무 콘솔
- 신뢰감 있는 데이터 검토 화면
- 과하지 않은 녹색 포인트
- 한국어 실무 문장 중심

## Avoid

- 공공기관 민원포털처럼 딱딱하고 회색 위주의 화면
- ESG 홍보물처럼 나뭇잎, 지구, 초록 그라데이션을 과하게 쓰는 화면
- 엑셀을 그대로 웹으로 옮긴 듯한 빽빽한 표
- KPI 카드만 나열하고 다음 작업이 보이지 않는 대시보드
- 화려한 스타트업 랜딩페이지 느낌
- 사용자가 무엇을 해야 하는지 모르는 분석 화면

## Color Tokens

- `background`: `#F6F8F7`
- `surface`: `#FFFFFF`
- `primary`: `#0F766E`
- `primary-hover`: `#0D9488`
- `primary-light`: `#CCFBF1`
- `text-main`: `#111827`
- `text-sub`: `#6B7280`
- `border`: `#E5E7EB`
- `success`: `#10B981`
- `warning`: `#F59E0B`
- `danger`: `#EF4444`
- `info`: `#2563EB`

## Layout Rules

- 첫 화면은 “제출 준비 작업실”처럼 구성한다.
- 대시보드 상단에는 현재 상태 문장과 가장 중요한 다음 작업 CTA를 둔다.
- 주요 업무 흐름은 `WorkflowStepper` 또는 단계 카드로 표현한다.
- 반복 데이터는 테이블로 제공하되, 모바일에서는 카드 리스트로 바꾼다.
- 카드 안에 또 다른 장식용 카드를 중첩하지 않는다.
- 입력 화면은 데스크톱에서 편해야 하고, 모바일에서는 조회와 확인 중심으로 둔다.

## Core UI Patterns

### NextActionPanel

가장 중요한 미해결 작업 1개를 보여준다.

- 제목: 사용자가 해야 할 행동
- 이유: 왜 필요한지
- CTA: 바로 이동하는 버튼
- 상태: 오류, 확인 필요, 권장 작업

### WorkflowStepper

CBAM 제출 준비 흐름을 단계별로 보여준다.

- 사업장/보고기간
- 품목
- 생산공정
- 배출원 자료
- 전구물질
- 산정 결과
- 시나리오
- EU Export
- 백업

### FixCard

경고나 오류를 해결 가능한 작업 카드로 보여준다.

- 문제
- 영향
- 해결 방법
- 이동 버튼

### EvidenceChecklist

자료 준비 상태를 보여준다.

- CN 코드
- 생산량
- 연료 사용량
- 전력 사용량
- 배출계수
- 전구물질 구매량
- 공급사 SEE 자료
- EU 원본 템플릿
- `.cbam` 백업

### ExportGate

Export 화면의 핵심 패턴이다.

- 제출 가능 여부
- 오류와 경고
- EU 원본 템플릿 상태
- 공식 수식 보존 상태
- Excel 수식 재계산 확인 안내
- `.cbam` 백업 상태
- 제출용 복사본 생성 버튼

### LocalDataNotice

로컬 저장, 백업, 서버 미전송을 명확히 안내한다.

## Component Rules

- `PageHeader`: 페이지 목적과 완료 기준을 한 문장으로 설명한다.
- `SectionCard`: 하나의 업무 판단 단위만 담는다.
- `StatCard`: 숫자만 보여주지 말고 업무 상태 helper를 붙인다.
- `StatusBadge`: 색상만으로 상태를 구분하지 않고 반드시 텍스트를 포함한다.
- `DataTable`: 행 높이를 충분히 두고, 행 hover와 작업 버튼을 제공한다.
- `Button`: 주요 화면에서 primary CTA는 하나만 둔다.
- `AlertBox`: 문제보다 다음 행동을 먼저 보이게 쓴다.
- `EmptyState`: 빈 화면에는 다음 입력 버튼을 제공한다.

## Korean Copy Rules

- UI 기본 언어는 한국어다.
- 전문 용어는 유지하되 행동 중심 문장으로 풀어 쓴다.
- “오류”보다 “수정 필요”, “확인 필요”, “다음 작업”을 우선 사용한다.
- 공식 EU 시트명, 셀명, 약어, 제출 양식명은 필요한 경우 영어 원문을 유지한다.
- EU 원본 템플릿의 영어 문구, 시트명, 공식 수식은 번역하거나 변경하지 않는다.

## License And Admin UX Rules

- 무료 라이선스는 배포 관리, 공지, 업데이트, 문의 대응을 위한 장치다.
- 라이선스 UI는 CBAM 산정 데이터가 서버로 전송되지 않는다는 점을 명확히 설명해야 한다.
- 관리자 콘솔은 사용자, 라이선스, 공지, 업데이트 정책만 관리한다.
- 관리자 콘솔은 생산량, 배출량, 전구물질, 공급업체 자료, EU 템플릿, `.cbam` 백업을 보거나 저장하지 않는다.

