# 개선 제안 — run12 (회귀 · 신규 결함만)

> **2026-09-18 추가 — 신규 P1 1건·P2 9건 수정** (브랜치 `fix/run12-followups`). `npm run verify` 통과. P1-run12-01은 실제 앱에서 확인: 대만으로 기본값을 채운 뒤 한국으로 바꾸면 값·출처·사유가 비워지고 "공급국가를 바꿨습니다 … 다시 누르세요"가 뜬다.
> 잔존 관찰 중 3건도 함께 처리: /results의 스크랩 라인 라벨(「활동수준 제외 (신고 대상 아님)」), Export 체크리스트의 "0건을 검토" 문장, 4단계에서 유형을 바꿔도 남던 오류 문구.
P0 0건 · P1 1건 · P2 9건. run11 항목 중 FAIL은 P2-33(부분) 1건 — 아래 P2-run12-01과 같은 내용.

## P0
없음.

## P1
### P1-run12-01 — 6단계에서 공급국가를 바꿔도 이미 채운 기본값·출처·사유가 옛 나라 값으로 남는다
재현: 6단계 → CN 72230019 → 공급국가 Taiwan → 「공급사 자료 없음 — EU 기본값 채우기」(직접 11, 출처 "… / Taiwan / 722300") → 공급국가를 South Korea로 변경. 직접 칸 11, 출처 Taiwan, 사유 "직접 11 …"이 그대로다. 버튼을 다시 눌러야 4.015로 바뀐다. 이 상태로 「원료 저장」을 누르면 국가 KR + 값 11(또는 반대로 TW + 4.015)이 저장될 수 있다(저장은 실행하지 않음 — 원장 보호). P0-04가 막으려던 "국가와 값의 불일치"가 다른 경로로 남는다.
방향: 국가 변경 시 DEFAULT로 채운 값·출처·사유를 비우거나 자동 재조회, 최소한 "국가가 바뀌었습니다 — 기본값을 다시 채우세요" 안내.

## P2
| ID | 무엇 | 재현·원문 |
|---|---|---|
| P2-run12-01 | /upload 「기본값 조회」에 부동소수 꼬리 | South Korea · 72230019 → 2026 칸 `4.015000000000001`. 6단계는 4.015로 고쳤는데 새 조회 화면이 같은 꼬리를 낸다(P2-33 부분 FAIL) |
| P2-run12-02 | 4단계 순발열량 도움말에 마크다운 별표 노출 | "고지서의 MJ는 \*\*총발열량\*\*(약 42~43 MJ/Nm³)이라 …" |
| P2-run12-03 | 7단계 「해결하면 좋은 항목」 중 사업장 2건은 링크가 아니다 | 생산공정·전구물질 항목은 `?edit=` 링크, 사업장(법정 필수·UN/LOCODE)은 글자만. 문구는 "사업장 화면에서 채우세요" |
| P2-run12-04 | Word 9.1 주석·9.2가 기본값 전구물질을 「실측」이라 부른다 | "STS 304 CHQ 와이어 — Feng-Yuan …: CN 72230019 **실측값**을 상위 heading(CN 722300) 기준 DV와 비교했습니다", 9.2 "실측 채택 (본 산정)". 표 라벨(P1-19)은 고쳐졌으나 주석 문장은 그대로 |
| P2-run12-05 | 시나리오 톤 입력이 2자리 %로 반올림 저장된다 | 620 t → 19.14% → 표 "EU 620.136", 인증서 1,506.3347(정확히는 1,506.0). 고객사에 보여줄 때 620이 아닌 수가 나온다 |
| P2-run12-06 | 5단계 "미리 채워진 0.47은 임시 자리값입니다" 문구가 0.4747 저장 후에도 계속 뜬다 | 담당자 값이 자리값 취급받는 것처럼 읽힌다. 값이 0.47일 때만 표시 |
| P2-run12-07 | 진행률 분모와 6·7단계 표시가 오락가락 | 빈 프로젝트 「0 / 6」→ 5단계 후 「6 / 7」(6단계 미완인데 7단계 ✓ · 기준 0.01) → 「강재 없음」 체크 「6 / 6」. 체크한 뒤 6단계 문구가 "구매한 CBAM 강재가 있으면"으로 돌아가 확인했다는 흔적이 지도에 없다 |
| P2-run12-08 | 경유(L)로 넣은 항목을 수정하면 「유류·기타 (t)」 10.354로 열린다 | 값은 맞지만 이름은 "경유 (L)…", 단위는 t, 원래 12,400 L은 어디에도 남지 않는다. 주유 전표와 대조할 때 역산해야 한다 |
| P2-run12-09 | 국가 목록에 잘린 이름 | "Democratic Republic of the Cong"(Excel 시트명 31자 제한) — 6단계·/upload 선택지 |

## 잔존 관찰 (신규 아님)
- /results에서 탄소강 공정의 스크랩 라인(90 t)이 여전히 「CBAM 신고 대상」 라벨(run11 W3-07). Export에는 나가지 않는다.
- /guide 흐름도는 데이터가 있어도 「예시 데이터」이면서 "입력하면 우리 회사 값으로 바뀝니다"라고 쓴다.
- /export 체크리스트 "기본값 우위 0건, 기본값 대비 차이 0건을 검토해야 합니다" — 0건을 검토하라는 문장.
- 탄소강 탭 7단계 「신고 대상 확인 필요」는 비신고 공정임을 아는 담당자에게 할 일처럼 읽힌다.
- 4단계에서 유형을 바꿔도 직전 오류 문구가 남는다.
