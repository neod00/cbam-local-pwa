# run13 개선 제안 — 「저장하고 다시 열기」 회귀

- **일자**: 2026-09-19 · **대상**: main @0b025cc (PR #52~#59 머지 후)
- **범위**: 무변경 저장(A1~A9), 백업 왕복(C1·C2), 삭제·기간 추가(B1~B4). 수출 파일 점검은 세션 종료로 미완.
- **통과**: /installations, /products, /source-streams, /precursors, 지도 1·2·4·5단계, /report-inputs, /scenarios 무변경 저장 diff 0. 백업 왕복 diff 0. 제품·공정 삭제 차단.

| # | 등급 | 증상 | 조치 |
|---|---|---|---|
| 01 | P0 | /processes에서 제품 라인을 지우고 다시 넣으면 기준 SEE 3.764가 0.022가 되고 8단계가 열린다. 전구물질 배분이 지워진 라인 id를 가리킨 채 남는다. | 배분이 걸린 라인은 저장 단계에서 삭제 차단. 엔진 경고와 내보내기 오류 추가. |
| 02 | P1 | 제품 라인이 1개일 때 지도 6단계를 그대로 저장하면 output_allocations가 사라진다. 뒤에 라인이 늘면 배분이 조용히 바뀐다(3.764에서 3.757). | 배분 UI가 안 보이는 상태에서는 기존 배분을 보존. |
| 03 | P1 | 추가했던 기간을 지우면 export:reporting-period가 없는 기간을 가리켜 8단계가 잠기고 풀 방법이 없다. | 없는 기간을 가리키는 선택은 무시. 기간 삭제 시 선택도 지움. |
| 04 | P1 | /export가 1단계에서 고른 기간을 읽지 않는다. 자료 0건인 기간을 골라도 문서가 열린다. | /export가 선택을 읽고 직접 고를 수도 있게 함. 빈 기간은 오류. |
| 05 | P1 | 지도 3단계에서 비CBAM 제품만 만드는 공정을 열면 라인이 안 보이고 저장이 막힌다. | 보존되는 라인을 안내하고 저장 허용. |
| 06 | P2 | 지도 3단계 저장이 라인 이름, reporting_scope, 비고, 수동 배분율을 덮어쓴다. | 기존 라인은 생산량만 갱신. |
| 07 | P2 | 새 프로젝트가 벤치마크 파일과 보고서 입력값을 지우는데 확인창이 말하지 않는다. | 벤치마크는 보존. 확인창에 지워지는 것 명시. |
| 08 | P2 | 직접배출 합계에 부동소수 꼬리(1049.9665725000002). | 합계를 소수 9자리로 정리. |
| 09 | P2 | 기간이 2개일 때 어디서 고르는지 안내가 없다. | 오류 문구에 위치 추가. |

## 부수 발견
- Windows에서 main을 체크아웃하면 소스가 CRLF로 바뀌어 검증 스크립트(`'\n}\n'`로 소스를 자름)가 깨진다. `.gitattributes`로 LF 고정.

## 수정본 화면 확인 (2026-09-19 오후, fix/run13-followups)

Playwright MCP가 넘기는 플래그 조합에서 Chrome 152가 기동 직후 죽는다(0xC0000005). 같은 프로필의 Chrome을 직접 띄우고 CDP로 연결해 실제 클릭·입력으로 확인했다. IndexedDB는 읽기만 했다.

| # | 확인 | 결과 |
|---|---|---|
| 01 | /processes STS 공정에서 「STS 나사 완제품」 라인 휴지통 후 저장 | 차단 문구 표시, 폼 유지, 원장 불변(3.764) |
| 02 | 지도 6단계 대만 와이어 무변경 저장 | updated_at 갱신, output_allocations 610 t · 100% 유지 |
| 03 | 2026 기간 추가 후 /export에서 2026 선택, 지도에서 2026 삭제 | 설정이 비워지고 8단계 열림, 3.764 |
| 04 | /export 기간 선택칸 | 기간 2개일 때 표시. 2026(자료 0건) 선택 시 오류, 2025 선택 시 기간 오류 0건이고 지도도 열림 |
| 05 | 지도 3단계 탄소강 공정 무변경 저장 | 보존 안내 표시, 저장됨. 라인 4개 저장 전후 동일 |
| 06 | 지도 3단계 STS 공정 무변경 저장 | 라인 이름·범위·배분율 동일 |
| 09 | 기간 2개일 때 7단계 문구 | 고르는 위치 표시. 화면 이름을 실제 메뉴명 「상세 Export」로 고침 |

07·08은 화면 확인을 하지 않았다(코드 게이트만).

## run13 잔여 검사
- 수출 파일: 수정 후 지도 8단계에서 받은 사본(04-eu-copy-after-fix.xlsx, 입력 셀 72개)이 수정 전 사본(02-eu-copy.xlsx)과 19개 시트 전 셀에서 같다.
- 옛 run11 백업(2026-09-16) 가져오기: 복원됨, settings 4건(key 중복 없음), 6/6 완료, 기준 3.575(그 백업 당시 자료의 값), 확인 항목 5건.
- 종료 시 00-baseline.cbam 복원: 3.764 · 확인 3건 · settings 4건.
- 산정보고서(03-report.docx) 내용 점검은 하지 않았다.
